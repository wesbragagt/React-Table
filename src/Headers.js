import CreateColumn from './CreateColumn';
const Headers = [
    new CreateColumn('Name', 'name'),
    new CreateColumn('Age', 'age'),
    new CreateColumn('Type', 'type'),
    new CreateColumn('Job', 'job')
];

export default Headers;
